[package]
name = "red-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "RedBrowser wire protocol — message types for browser, Instagram, Telegram, WhatsApp over DNS tunnel"

[dependencies]
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
